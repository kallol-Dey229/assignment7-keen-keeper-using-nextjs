'use client';

const error = () => {
    return (
        <div>
            <h2 className='text-red-700 flex items-center justify-center'>Error occurred while fetching friends</h2>
        </div>
    );
};

export default error;